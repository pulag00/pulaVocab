{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build pulaVocab",
            "type": "shell",
            "command": "dotnet",
            "args": [
                "build",
                "pulaVocab.sln"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$msCompile"
            ]
        }
    ]
}